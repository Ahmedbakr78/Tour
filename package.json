{
  "name": "tourmate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TourMate — MEAN-stack smart tourism platform. Vercel full-stack (serverless API + Angular) + Cloudflare Pages frontend (proxies /api to Vercel).",
  "scripts": {
    "build": "cd client && npm install --legacy-peer-deps && npm run build",
    "vercel-build": "cd client && npm install --legacy-peer-deps && npm run build",
    "start": "node server/src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb-memory-server": "^11.2.0",
    "mongoose": "^8.5.1",
    "morgan": "^1.11.0",
    "multer": "^1.4.5-lts.1",
    "swagger-ui-express": "^5.0.1"
  }
}
